import IQuote from "../IQuote";

function Quotes({quotes, filters, addFilter}:{quotes: IQuote[], filters: string[], addFilter: Function}){
    console.log(quotes)
    return (
        <div></div>
    )
}
export default Quotes;